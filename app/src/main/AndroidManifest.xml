<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="b05studio.com.order_boss">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.RestaurantActivity" />
        <activity android:name=".view.activity.ReservationActivity" />
        <activity android:name=".view.activity.SettingActivity" />
        <activity android:name=".view.activity.MyRecordActivity" />
        <activity android:name=".view.activity.SearchActivity" />
        <activity android:name=".view.activity.SearchResultActivity" />
    </application>

</manifest>